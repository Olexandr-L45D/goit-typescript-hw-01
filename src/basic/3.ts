// 3

let variableValue: [string | number];

type ValueActiv = {
    isActiv: 'enable';
    notActiv: 'disable';
};
// console.log(ValueActiv(isActiv));

// type varValue = [string | number];

// const valueActive: varValue = {
//     isActiv: 'enable',
//     notActiv: 'disable',
// };







// type usProfil = {
//     usName: string;
//     ages: number;
//     isActiv: boolean;
// };

// function displayUsProfil(user: usProfil) {
//     return `This User name ${user.usName}, tnis ${user.ages} eas old and is isActiv ${user.isActiv}`

// };

// const user = {
//     usName: 'Olena',
//     ages: 32,
//     isActiv: true,
// };
// console.log(displayUsProfil(user));
